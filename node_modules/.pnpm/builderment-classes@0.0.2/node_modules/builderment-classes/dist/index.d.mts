import SelfMap from 'selfmap';

/**
 * @author Space.yg
 */
/** The area parameters */
type AreaParams = {
    /** The width. */
    width: number;
    /** The height. */
    height: number;
};
/** Create a new area. */
declare class Area {
    /** The width. */
    width: number;
    /** The height. */
    height: number;
    /**
     * Area.
     * @readonly
     */
    get area(): number;
    /**
     * Constructs a new {@link Area `Area`} object.
     * @param params The area parameters.
     */
    constructor(params: AreaParams);
    /**
     * Constructs a new {@link Area `Area`} object.
     * @param area An {@link Area `Area`} object.
     */
    constructor(area: Area);
    /**
     * Determine if this {@link Area `Area`} object is equal to another {@link Area `Area`} object.
     * @param area The other {@link Area `Area`} object.
     * @returns `true` if both {@link Area `Area`} objects are equal, `false` otherwise.
     */
    equals(area: Area): boolean;
}

/**
 * @author Space.yg
 */
/** The price params. */
type PriceParams = {
    /**
     * The amount of gold.
     * @default 0
     */
    gold?: number | null;
    /**
     * The amount of gems.
     * @default 0
     */
    gems?: number | null;
};
/** Make a new price. */
declare class Price {
    /**
     * Add multiple prices together.
     * @param prices Prices to add.
     * @returns Total price.
     */
    static add(...prices: (Price | PriceParams)[]): Price;
    /**
     * Determine if all prices are equal to each other.
     * @param prices The prices to compare.
     * @returns `true` if all prices are equal, `false` otherwise.
     */
    static equalPrices(...prices: (Price | PriceParams)[]): boolean;
    /**
     * The amount of gold.
     * @default 0
     */
    gold: NonNullable<PriceParams["gold"]> | null;
    /**
     * The amount of gems.
     * @default 0
     */
    gems: NonNullable<PriceParams["gems"]> | null;
    /**
     * Construct a {@link Price `Price`} object.
     */
    constructor();
    /**
     * Construct a {@link Price `Price`} object.
     * @param params The price parameters
     */
    constructor(params: PriceParams);
    /**
     * Construct a {@link Price `Price`} object.
     * @param price A {@link Price `Price`} object
     */
    constructor(price: Price);
    /**
     * Converts this {@link Price `Price`} object to a string.
     */
    toString(): string;
    /**
     * Add two {@link Price `Price`} objects together. This method mutates the original {@link Price `Price`} object.
     * @param price The other {@link Price `Price`} object to add.
     * @returns The original {@link Price `Price`} object.
     */
    add(price: Price): this;
    /**
     * Add two {@link Price `Price`} objects together. This method creates a new {@link Price `Price`} object.
     * @param price The other {@link Price `Price`} object to add.
     * @returns A new {@link Price `Price`} object.
     */
    toAdd(price: Price): Price;
    /**
     * Determine if this {@link Price `Price`} object and another {@link Price `Price`} object are the equal.
     * @param price The other price.
     * @returns `true` if both prices are the equal, `false` otherwise.
     */
    equals(price: Price): boolean;
    /**
     * Called by `Object.prototype.toString`.
     */
    get [Symbol.toStringTag](): string;
    /**
     * Iterator called by the for-of loop.
     */
    [Symbol.iterator](): Generator<number | null, void, unknown>;
}

/**
 * @author Space.yg
 */

/** The base parameters. */
type BaseParams = {
    /** The name. */
    name: string;
    /** The price. */
    price: Price | PriceParams;
    /**
     * URL or relative path to the image.
     * @default "./resources/"
     */
    image?: string;
};
/** The base of some classes. */
declare class Base {
    /**
     * All the bases that has been created.
     * @readonly
     */
    static readonly bases: {
        [/** The name. */ name: string]: Base[];
    };
    /** The name. */
    name: BaseParams["name"];
    /** The price. */
    price: Price;
    /**
     * URL or relative path to the image.
     * @default "./resources/"
     */
    image: NonNullable<BaseParams["image"]>;
    /**
     * Constructs a {@link Base `Base`} object.
     * @param params The base parameters.
     */
    constructor(params: BaseParams);
    /**
     * Constructs a {@link Base `Base`} object.
     * @param base A {@link Base `Base`} object.
     * @param passByReference Whether to pass the objects in {@link base `base`} by reference or not. Default is `true`.
     */
    constructor(base: Base, passByReference?: boolean);
    /**
     * Constructs a {@link Base `Base`} object.
     * @param base A {@link Base `Base`} object or base parameters.
     * @param passByReference Whether to pass the objects in {@link base `base`} by reference or not. Default is `true`.
     */
    constructor(base: Base | BaseParams, passByReference?: boolean);
    /**
     * Converts this {@link Base `Base`} object into string.
     * @param limit The limit of how many tabs can be used. `limit` must be greater than 0. Default is 2.
     * @returns The string.
     */
    toString(limit?: number): string;
    /**
     * Determine if this {@link Base `Base`} object is equal to another {@link Base `Base`} object.
     * @param base The other {@link Base `Base`} object.
     * @returns `true` if both {@link Base `Base`} objects are equal, `false` otherwise.
     */
    equals(base: Base): boolean;
    /**
     * Determine if this {@link Base `Base`} object is strictly equal to another {@link Base `Base`} object.
     * @param base The other {@link Base `Base`} object.
     * @returns `true` if both {@link Base `Base`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(base: Base): boolean;
}

/**
 * @author Space.yg
 */

/** The resources type for all resources in a seed */
type ResourcesParams = {
    /** The amount of Wood Log deposits in the seed */
    "Wood Log": number;
    /** The amount of Stone deposits in the seed */
    Stone: number;
    /** The amount of Iron Ore deposits in the seed */
    "Iron Ore": number;
    /** The amount of Copper Ore deposits in the seed */
    "Copper Ore": number;
    /** The amount of Coal deposits in the seed */
    Coal: number;
    /** The amount of Wolframite deposits in the seed */
    Wolframite: number;
    /** The amount of Uranium Ore deposits in the seed */
    "Uranium Ore": number;
};
/** The Advanced World number options for World Size and Resource Amount when creating a new seed */
type AdvancedWorldSetting = 50 | 75 | 100 | 150 | 200;
/** The parameters of the item type */
type SeedParams = {
    /** The resources in a seed */
    resources: ResourcesParams;
    /**
     * The World Size of the seed
     * @default 100
     */
    worldSize?: AdvancedWorldSetting;
    /**
     * The Resource Amount of the seed
     * @default 100
     */
    resourceAmount?: AdvancedWorldSetting;
    /** The seed */
    seed?: string;
};
/** Make a new seed. */
declare class Seed {
    /**
     * Keep track of newly created seeds in the {@link seeds seeds} static variable
     * @default false
     */
    static keepTrackOfSeeds: boolean;
    /**
     * All the seeds that has been created with a seed.
     * @readonly
     */
    static readonly seeds: SelfMap<Seed, "seed">;
    /**
     * Get the amount of each raw resource needed for it to NOT be the limited resource
     * @param item The {@link Item `Item`} object to calculate the limited resource based on the base resource of that item
     * @param resources The resources of the seed to calculate the limited resources
     * @returns The amount of each raw resource needed for it to NOT be the limited resource
     */
    static getLimitedDeposits(item: Item, resources: ResourcesParams): InputOptions[];
    /**
     * Get the resource that limits making more of the item in the seed
     * @param item The {@link Item `Item`} object to get the resources that limits making more of this item
     * @param resources The resources in the seed
     * @returns The limited resource
     */
    static getLimitedDeposit(item: Item, resources: ResourcesParams): InputOptions;
    /**
     * **This function is incomplete. Do not use.**
     *
     * Construct a {@link Seed} object from a world save file
     * @param path The path to the world save file
     * @returns A {@link Seed} object
     */
    static constructFromFile(path: string): Promise<Seed>;
    /** The resources in a seed. */
    resources: SeedParams["resources"];
    /**
     * The world size of the seed.
     * @default 100
     */
    worldSize: NonNullable<SeedParams["worldSize"]>;
    /**
     * The resource amount of the seed.
     * @default 100
     */
    resourceAmount: NonNullable<SeedParams["resourceAmount"]>;
    /** The seed. */
    seed?: SeedParams["seed"];
    /**
     * Construct a {@link Seed} object.
     * @param params The seed parameters.
     */
    constructor(params: SeedParams);
    /**
     * Construct a {@link Seed} object.
     * @param seed A {@link Seed} object.
     * @param passByReference Whether to pass the objects in {@link seed `seed`} by reference or not. Default is `true.`
     */
    constructor(seed: Seed, passByReference?: boolean);
    /**
     * Construct a {@link Seed} object.
     * @param seed A {@link Seed} object or seed parameters.
     * @param passByReference Whether to pass the objects in {@link seed `seed`} by reference or not. Default is `true.`
     */
    constructor(seed: Seed | SeedParams, passByReference?: boolean);
    /**
     * Converts the seed into string
     * @param limit The limit of how many tabs can be used. `limit` must be greater than 0. Default is 2
     * @returns The string
     */
    toString(limit?: number): string;
    /**
     * Get the amount of each raw resource needed for it to NOT be the limited resource
     * @param item The {@link Item `Item`} object to calculate the limited resource based on the base resource of that item
     * @returns The amount of each raw resource needed for it to NOT be the limited resource
     */
    getLimitedDeposits(item: Item): InputOptions[];
    /**
     * Get the resource that limits making more of the item in the seed
     * @param item The {@link Item `Item`} object to get the resources that limits making more of this item
     * @returns The limited resource
     */
    getLimitedDeposit(item: Item): InputOptions;
    /**
     * Get the maximum amount to make an item in the seed
     * @param item The {@link Item `Item`} object to get the max of
     * @returns The maximum amount to make the item in the seed
     */
    getMax(item: Item): number;
    /**
     * Determine if this seed and another seed are the equal
     * @param seed The other seed
     * @returns true if both seeds are the equal, false otherwise
     */
    equals(seed: Seed): boolean;
    /**
     * Determine if this seed and another seed are the strictly equal
     * @param seed The other seed
     * @returns true if both seeds are the strictly equal, false otherwise
     */
    strictlyEquals(seed: Seed): boolean;
}

/**
 * @author Space.yg
 */

/** Optional parameters for a tier */
type TierParams = {
    /**
     * The price of that tier.
     * @default
     * ```javascript
     * new Price({
     * 	gold: 0,
     * 	gems: 0,
     * })
     * ```
    */
    price?: Price | PriceParams;
    /**
     * The relative path or URL to the image of this tier.
     * @default "./resources/"
     */
    image?: string;
};
/** Make a new tier. */
declare class Tier {
    /**
     * The price of that tier.
     * @default
     * new Price({
     * 	gold: 0,
     * 	gems: 0,
     * })
     */
    price: Price;
    /**
     * The relative path or URL to the image of this tier.
     * @default "./resources/"
     */
    image: NonNullable<TierParams["image"]>;
    /**
     * Construct a {@link Tier `Tier`} object.
     * @param params The tier parameters.
     */
    constructor(params: TierParams);
    /**
     * Construct a {@link Tier `Tier`} object.
     * @param tier A {@link Tier `Tier`} object.
     */
    constructor(tier: Tier);
    /**
     * Converts the tier into string.
     * @param limit The limit of how many tabs can be used. `limit` must be greater than `0`. Default is `2`.
     * @returns The string.
     */
    toString(limit?: number): string;
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param tier The other {@link Tier `Tier`} object.
     * @returns `true` if both {@link Tier `Tier`} objects are the equal in the properties that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(tier: Tier): boolean;
    /**
     * Determine if this {@link Tier `Tier`} object is equal to another {@link Tier `Tier`} object.
     * @param tier The other {@link Tier `Tier`} object.
     * @returns `true` if both {@link Tier `Tier`} objects are equal, `false` otherwise.
     */
    equals(tier: Tier): boolean;
    /**
     * Determine if this {@link Tier `Tier`} object is strictly equal to another {@link Tier `Tier`} object.
     * @param tier The other {@link Tier `Tier`} object.
     * @returns `true` if both {@link Tier `Tier`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(tier: Tier): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for {@link Tiers `Tiers`}. The `tier`s **must** be consecutive integers.
 * @template P The parameters
 * @template T The tier
 */
type TiersParams<P extends TierParams, T extends Tier> = {
    [/** The tiers. They must be consecutive integers. */ tier: number]: P | T;
};
/** Make a new tiers. */
declare class Tiers {
    /** All tiers. */
    tiers: {
        [/** The tiers. They must be consecutive integers. */ tier: number]: Tier;
    };
    /**
     * The total price to get to the maximum tier.
     * @readonly
     */
    get maxPrice(): Price;
    /**
     * Maximum tier number.
     * @readonly
     */
    get maxTierNum(): number;
    /**
     * Minimum tier number.
     * @readonly
     */
    get minTierNum(): number;
    /**
     * Maximum tier.
     * @readonly
     */
    get maxTier(): Tier;
    /**
     * Minimum tier.
     * @readonly
     */
    get minTier(): Tier;
    /**
     * Constructs a {@link Tier `Tier`} object.
     * @param params The tiers parameters.
     * @param passByReference Whether to pass the objects in the {@link Tier `Tier`} by reference or not. Default is `true`.
     */
    constructor(params: TiersParams<TierParams, Tier>, passByReference?: boolean);
    /**
     * Constructs a {@link Tier `Tier`} object.
     * @param tiers A {@link Tier `Tier`} object.
     * @param passByReference Whether to pass the objects in the {@link Tier `Tier`} by reference or not. Default is `true`.
     */
    constructor(tiers: Tiers, passByReference?: boolean);
    /**
     * Constructs a {@link Tier `Tier`} object.
     * @param tiers A {@link Tier `Tier`} object or tiers parameters.
     * @param passByReference Whether to pass the objects in the {@link Tier `Tier`} by reference or not. Default is `true`.
     */
    constructor(tiers: Tiers | TiersParams<TierParams, Tier>, passByReference?: boolean);
    /**
     * Converts the tiers into string.
     * @param limit The limit of how many tabs can be used. `limit` must be greater than `0`. Default is `2`.
     * @returns The string.
     */
    toString(limit?: number): string;
    /**
     * Check if a tier number is in this {@link Tiers `Tiers`} object's tiers.
     * @param tier Tier number to search for.
     * @param error Whether to throw an error.
     * @throws Error if tier number was not found.
     * @returns Whether the tier number is in this {@link Tiers `Tiers`} object's tiers.
     */
    hasN(tier: number, error?: boolean): boolean;
    /**
     * Check if this {@link Tiers `Tiers`} object has a {@link Tier `Tier`} object as one of its tiers.
     * @param tier The {@link Tier `Tier`} object to check.
     * @returns `true` if this {@link Tiers `Tiers`} object has {@link tier `tier`} as one of its tiers, `false` otherwise.
     */
    has(tier: Tier): boolean;
    /**
     * Check if this {@link Tiers `Tiers`} object strictly has a {@link Tier `Tier`} object as one of its tiers.
     * @param tier The {@link Tier `Tier`} object to check.
     * @returns `true` if this {@link Tiers `Tiers`} object strictly has {@link tier `tier`} as one of its tiers, `false` otherwise.
     */
    strictlyHas(tier: Tier): boolean;
    /**
     * Check if this {@link Tiers `Tiers`} object has a reference to {@link Tier `Tier`} object as one of its tiers.
     * @param tier The {@link Tier `Tier`} object to check.
     * @returns `true` if this {@link Tiers `Tiers`} object has a reference to {@link tier `tier`} as one of its tiers, `false` otherwise.
     */
    hasReference(tier: Tier): boolean;
    /**
     * Get the total price to get to a tier.
     * @param tier The tier.
     */
    getTotalPrice(tier: number): Price;
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param tiers The other {@link Tiers `Tiers`} object.
     * @returns `true` if both {@link Tiers `Tiers`} objects are the equal in the properties that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(tiers: Tiers): boolean;
    /**
     * Determine if this {@link Tiers `Tiers`} object is equal to another {@link Tiers `Tiers`} object.
     * @param tiers The other {@link Tiers `Tiers`} object.
     * @returns `true` if both tiers are equal, `false` otherwise.
     */
    equals(tiers: Tiers): boolean;
    /**
     * Determine if this {@link Tiers `Tiers`} object is strictly equal to another {@link Tiers `Tiers`} object.
     * @param tiers The other {@link Tiers `Tiers`}.
     * @returns `true` if both tiers are strictly equal, `false` otherwise
     */
    strictlyEquals(tiers: Tiers): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for a output tier.
 * @extends {{@link TierParams `TierParams`}
 */
type OutputTierParams = TierParams & {
    /** The output at this tier. */
    output: number;
};
/**
 * An output tier.
 * @extends {{@link Tier `Tier`}
 */
declare class OutputTier extends Tier {
    /** The output at this tier. */
    output: OutputTierParams["output"];
    /**
     * Constructs a new {@link OutputTier `OutputTier`} object.
     * @param params The output tier parameters.
     */
    constructor(params: OutputTierParams);
    /**
     * Constructs a new {@link OutputTier `OutputTier`} object.
     * @param outputTier A {@link OutputTier `OutputTier`} object.
     */
    constructor(outputTier: OutputTier);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param outputTier The other {@link OutputTier `OutputTier`} object.
     * @returns `true` if both {@link OutputTier `OutputTier`} objects are the equal in the properties that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(outputTier: OutputTier): boolean;
    /**
     * Determine if this {@link OutputTier `OutputTier`} object is equal to another {@link OutputTier `OutputTier`} object.
     * @param outputTier The other {@link OutputTier `OutputTier`} object.
     * @returns `true` if both {@link OutputTier `OutputTier`} objects are equal, `false` otherwise.
     */
    equals(outputTier: OutputTier): boolean;
    /**
     * Determine if this {@link OutputTier `OutputTier`} object is strictly equal to another {@link OutputTier `OutputTier`} object.
     * @param outputTier The other {@link OutputTier `OutputTier`} object.
     * @returns `true` if both {@link OutputTier `OutputTier`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(outputTier: OutputTier): boolean;
}

/**
 * @author Space.yg
 */

/**
 * The parameters of the optional output tiers.
 * @extends {{@link TiersParams `TiersParams`}
 */
type OutputTiersParams = TiersParams<OutputTierParams, OutputTier>;
/**
 * Make a new output tier.
 * @extends {{@link Tiers `Tiers`}
 */
declare class OutputTiers extends Tiers {
    tiers: {
        [/** The tiers. They must be consecutive integers. */ tier: number]: OutputTier;
    };
    get maxTier(): OutputTier;
    get minTier(): OutputTier;
    /**
     * Constructs a new {@link OutputTiers `OutputTiers`} object.
     * @param params The output tier parameters.
     * @param passByReference Whether to pass the objects in the {@link params `params`} by reference or not. Default is `true`.
     */
    constructor(params: OutputTiersParams, passByReference?: boolean);
    /**
     * Constructs a new {@link OutputTiers `OutputTiers`} object.
     * @param outputTiers An {@link OutputTiers `OutputTiers`} object.
     * @param passByReference Whether to pass the objects in the {@link outputTiers `outputTiers`} by reference or not. Default is `true`.
     */
    constructor(outputTiers: OutputTiers, passByReference?: boolean);
    /**
     * Constructs a new {@link OutputTiers `OutputTiers`} object.
     * @param outputTiers An {@link OutputTiers `OutputTiers`} object or output tier parameters.
     * @param passByReference Whether to pass the objects in the {@link outputTiers `outputTiers`} by reference or not. Default is `true`.
     */
    constructor(outputTiers: OutputTiers | OutputTiersParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param outputTiers The other {@link OutputTiers `OutputTiers`} object.
     * @returns `true` if both {@link OutputTiers `OutputTiers`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(outputTiers: OutputTiers): boolean;
    /**
     * Determine if this {@link OutputTiers `OutputTiers`} object is equal to another {@link OutputTiers `OutputTiers`} object.
     * @param outputTiers The other {@link OutputTiers `OutputTiers`} object.
     * @returns `true` if both {@link OutputTiers `OutputTiers`} objects are equal, `false` otherwise.
     */
    equals(outputTiers: OutputTiers): boolean;
    /**
     * Determine if this {@link OutputTiers `OutputTiers`} object is strictly equal to another {@link OutputTiers `OutputTiers`} object.
     * @param outputTiers The other {@link OutputTiers `OutputTiers`} object.
     * @returns `true` if both {@link OutputTiers `OutputTiers`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(outputTiers: OutputTiers): boolean;
}

/**
 * @author Space.yg
 */

/** Parameters for {@link Factory `Factory`}. */
type FactoryParams = {
    /** The name of the factory. */
    name: string;
    /** The amount of inputs of the factory. */
    inputs: number;
    /** The description of the factory. */
    description: string;
    /** The tiers of the factory. */
    tiers: OutputTiers | OutputTiersParams;
};
/**
 * Create a new factory.
 * @extends {{@link OutputTiers `OutputTiers`}
 */
declare class Factory extends OutputTiers {
    /**
     * All the factories that has been created.
     * @readonly
     */
    static readonly factories: Factory[];
    /** The name of the factory. */
    name: FactoryParams["name"];
    /** The price of the first tier of the factory. */
    price: Price;
    /** The amount of inputs of the factory. */
    inputs: FactoryParams["inputs"];
    /** The description of the factory. */
    description: FactoryParams["description"];
    /**
     * Constructs a new {@link Factory `Factory`} object.
     * @param params The factory parameters.
     * @param passByReference Whether to pass the objects in the {@link Factory `Factory`} by reference or not. Default is `true`.
     */
    constructor(params: FactoryParams, passByReference?: boolean);
    /**
     * Constructs a new {@link Factory `Factory`} object.
     * @param factory A {@link Factory `Factory`} object.
     * @param passByReference Whether to pass the objects in the {@link Factory `Factory`} by reference or not. Default is `true`.
     */
    constructor(factory: Factory, passByReference?: boolean);
    /**
     * Constructs a new {@link Factory `Factory`} object.
     * @param factory A {@link Factory `Factory`} object or factory parameters.
     * @param passByReference Whether to pass the objects in the {@link Factory `Factory`} by reference or not. Default is `true`.
     */
    constructor(factory: Factory | FactoryParams, passByReference?: boolean);
    /**
     * These are similarities between the equals and strictlyEquals methods
     * @param factory The other factory
     * @returns true if both factories are the equal in the things that are similar between the equals and strictlyEquals methods, false otherwise
     */
    protected similarEquals(factory: Factory): boolean;
    /**
     * Determine if this factory and another factory are the equal
     * @param factory The other factory
     * @returns true if both factories are the equal, false otherwise
     */
    equals(factory: Factory): boolean;
    /**
     * Determine if this factory and another factory are the strictly equal
     * @param factory The other factory
     * @returns true if both factories are the strictly equal, false otherwise
     */
    strictlyEquals(factory: Factory): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for {@link Item `Item`}.
 * @extends {{@link BaseParams `BaseParams`}
 */
type ItemParams = BaseParams & {
    /** The factory that makes the item. */
    factory: Factory;
    /** The resources needed to make the item. */
    resourcesNeeded: InputOptions[];
    /**
     * The relative path or URL to the image of the recipe of the item.
     * @default ""
     */
    recipeImage?: string;
    /** The output/min of the item in tier 1 of the factory. */
    outputPerMin: number;
    /**
     * The amount of items produces each manufacture.
     *
     * Example: Copper wire's output amount is `2`.
     * @default 1
     */
    outputAmount?: number;
};
/**
 * Make a new item.
 * @extends {{@link Base `Base`}
 */
declare class Item extends Base {
    #private;
    /**
     * All the items that has been created.
     * @readonly
     */
    static readonly items: Item[];
    /**
     * Get Item objects from a name.
     * @param name The item to get.
     * @returns The items if found or `undefined` if not found.
     */
    static getItemsByName(name: string): Item[];
    /** The factory that makes the item. */
    factory: ItemParams["factory"];
    /** The image URL of the recipe of the item. */
    recipeImage: NonNullable<ItemParams["recipeImage"]>;
    /** The output/min of the item in tier 1 of the factory. */
    outputPerMin: ItemParams["outputPerMin"];
    /**
     * The amount of items produces each manufacture.
     *
     * Example: Copper wire's output amount is `2`.
     * @default 1
     */
    outputAmount: NonNullable<ItemParams["outputAmount"]>;
    /** The resources needed to make the item. */
    resourcesNeeded: InputMap;
    /** The base resources needed of 1 of the item. */
    baseResources: Partial<ResourcesParams>;
    /** Get the max output/min of the factory to make this item. */
    get maxOutputPerMin(): number;
    /** Get the resources needed to make the item at the max tier. */
    get maxResourcesNeeded(): InputMap;
    /**
     * Construct an {@link Item `Item`} object.
     * @param params The item parameters.
     */
    constructor(params: ItemParams, passByReference?: boolean);
    /**
     * Construct an {@link Item `Item`} object from a pre-existing object.
     * @param item An item object.
     */
    constructor(item: Item, passByReference?: boolean);
    /**
     * Construct an {@link Item `Item`} object.
     * @param item The item object or item parameters.
     */
    constructor(item: Item | ItemParams, passByReference?: boolean);
    /**
     * Check if this item needs another item in it's recipe tree.
     * @param name The name of the item to search for.
     * @returns `true` if found, `false` otherwise.
     */
    needs(name: string): boolean;
    /**
     * Check if this item needs another item in it's recipe tree.
     * @param item An Item object to search for.
     * @returns `true` if found, `false` otherwise.
     */
    needs(item: Item): boolean;
    /**
     * Get the amount of base resources used to make the item.
     * @param amount The amount of that resource. Default is 1.
     */
    getAmountOfBaseResources(amount?: number): Partial<ResourcesParams>;
    /**
     * Get the amount of resources used to make the item.
     * @param amount The amount of that resource. Default is 1.
     */
    getAmountOfResources(amount?: number): any;
    /**
     * Get the output/min based on the tier of the factory.
     * @param tier The tier to get the output of. Default is 1.
     */
    getOutputPerMin(tier?: number): number;
    /**
     * Get the resources needed to make the item at a tier
     * @param tier The tier to get the resources needed. Default is 1
     */
    getResourcesNeeded(tier?: number): InputMap;
    /**
     * Get the maximum amount you can get of this item in a seed.
     *
     * **Note:** This method only works with the normal in-game items.
     * @param seed A {@link Seed `Seed`} object.
     * @returns the maximum amount you can get of this item.
     */
    getMaxResourceAmountInSeed(seed: Seed): number;
    /**
     * Get the maximum amount you can get of this item in a seed.
     *
     * **Note:** This method only works with the normal in-game items.
     * @param resources The resources in the world.
     * @returns the maximum amount you can get of this item.
     */
    getMaxResourceAmountInSeed(resources: ResourcesParams): number;
    /**
     * Get an approximate of the maximum amount of this item that can be produced using Power Plants.
     *
     * Steps of how the process works:
     * 1. Coal Power Plants boost Uranium Extractors.
     * 2. Coal Power Plants boost Coal Extractors.
     * 3. Nuclear Power Plants power the rest of the deposits' Extractors.
     * 4. If Coal is not the limited resource, use it to boost the other resources.
     * 5. Calculate the maximum resource amount.
     *
     * **Note:** This method only works with the normal in-game items.
     * @param seed A {@link Seed `Seed`} object.
     * @returns The maximum amount of this item that can be produced using Power Plants.
     */
    getMaxResourceAmountInSeedWithPowerPlants(seed: Seed): number;
    /**
     * Get an approximate of the maximum amount of this item that can be produced using Power Plants.
     *
     * Steps of how the process works:
     * 1. Coal Power Plants boost Uranium Extractors.
     * 2. Coal Power Plants boost Coal Extractors.
     * 3. Nuclear Power Plants power the rest of the deposits' Extractors.
     * 4. If Coal is not the limited resource, use it to boost the other resources.
     * 5. Calculate the maximum resource amount.
     *
     * **Note:** This method only works with the normal in-game items.
     * @param resources The resources in the world.
     * @param resourceAmount The resource amount of the world.
     * @returns The maximum amount of this item that can be produced using Power Plants.
     */
    getMaxResourceAmountInSeedWithPowerPlants(resources: ResourcesParams, resourceAmount?: AdvancedWorldSetting): number;
    /**
     * Get an approximate of the maximum amount of this item that can be produced using Power Plants.
     *
     * Steps of how the process works:
     * 1. Coal Power Plants boost Uranium Extractors.
     * 2. Coal Power Plants boost Coal Extractors.
     * 3. Nuclear Power Plants power the rest of the deposits' Extractors.
     * 4. If Coal is not the limited resource, use it to boost the other resources.
     * 5. Calculate the maximum resource amount.
     *
     * **Note:** This method only works with the normal in-game items.
     * @param resourcesOrSeed A {@link Seed `Seed`} object or resources in the world.
     * @param resourceAmount The resource amount of the world. If a {@link Seed `Seed`} object is supplied to {@link resourcesOrSeed `resourcesOrSeed`}, then this parameter is ignored.
     * @returns The maximum amount of this item that can be produced using Power Plants.
     */
    getMaxResourceAmountInSeedWithPowerPlants(resourcesOrSeed: ResourcesParams | Seed, resourceAmount?: AdvancedWorldSetting): number;
    /**
     * These are similarities between the equals and strictlyEquals methods.
     * @param item The other item.
     * @returns `true` if both items are the equal in the things that are similar between the equals and strictlyEquals methods, `false` otherwise.
     */
    protected similarEquals(item: Item): boolean;
    /**
     * Determine if this {@link Item `Item`} object and another {@link Item `Item`} object are equal.
     * @param item The other {@link Item `Item`} object.
     * @returns `true` if both items are the equal, `false` otherwise.
     */
    equals(item: Item): boolean;
    /**
     * Determine if this {@link Item `Item`} object and another {@link Item `Item`} object are strictly equal.
     * @param item The other {@link Item `Item`} object.
     * @returns `true` if both items are the strictly equal, `false` otherwise.
     */
    strictlyEquals(item: Item): boolean;
}

/**
 * @author Space.yg
 */



/** The options of an input. */
type InputOptions = {
	/** The item of the input. */
	item: Item
	/** The amount needed to make the parent. */
	amount: number
}

/**
 * The options of an input with input/min.
 */
type InputPerMin = {
	/** The amount needed to make the parent. */
	amount: number
	/** Total input/min in tier 1. */
	inputPerMin: number
}

type InputItemPerMin = InputPerMin & {
	/** The item of the input. */
	item: Item
}

/** The options of an input object. */
type InputMap = Map<Item, InputPerMin>

/**
 * @author Space.yg
 */

/**
 * Options for {@link ItemCollector `ItemCollector`}.
 * @extends {{@link BaseParams `BaseOptions`}
 */
type ItemCollectorParams = BaseParams & {
    /** The description of the build. */
    description: string;
    /** The limit how many of the build can be build. */
    limit: number;
    /** The amount of inputs of the build. */
    inputs: number;
};
/**
 * An item collector.
 * @extends {{@link Base `Base`}
 */
declare class ItemCollector extends Base {
    /**
     * All the item collectors that has been created.
     * @readonly
     */
    static readonly itemCollectors: ItemCollector[];
    /** The description of the build. */
    description: ItemCollectorParams["description"];
    /** The limit how many of the build can be build. */
    limit: ItemCollectorParams["limit"];
    /** The amount of inputs of the build. */
    inputs: ItemCollectorParams["inputs"];
    /**
     * Construct an {@link ItemCollector `ItemCollector`} object.
     * @param options The item collector options.
     */
    constructor(options: ItemCollectorParams);
    /**
     * Construct an {@link ItemCollector `ItemCollector`} object.
     * @param itemCollector An {@link ItemCollector `ItemCollector`} object.
     */
    constructor(itemCollector: ItemCollector);
    /**
     * Construct an {@link ItemCollector `ItemCollector`} object.
     * @param itemCollector an {@link ItemCollector `ItemCollector`} object or item collector options.
     */
    constructor(itemCollector: ItemCollector | ItemCollectorParams);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param itemCollector The other {@link ItemCollector `ItemCollector`} object.
     * @returns `true` if both {@link ItemCollector `ItemCollector`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    private similarEquals;
    /**
     * Determine if this {@link ItemCollector `ItemCollector`} object is equal to another {@link ItemCollector `ItemCollector`} object.
     * @param itemCollector The other {@link ItemCollector `ItemCollector`} object.
     * @returns `true` if both {@link ItemCollector `ItemCollector`} objects are equal, `false` otherwise.
     */
    equals(itemCollector: ItemCollector): boolean;
    /**
     * Determine if this {@link ItemCollector `ItemCollector`} object is strictly equal to another {@link ItemCollector `ItemCollector`} object.
     * @param itemCollector The other {@link ItemCollector `ItemCollector`} object.
     * @returns `true` if both {@link ItemCollector `ItemCollector`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(itemCollector: ItemCollector): boolean;
}

/**
 * @author Space.yg
 */

/** The build and tier of the build. */
type BuildTierOptions = {
    /** The build that will unlock it's tier */
    build: Tiers;
    /** The tier of the factory it will be unlocked at */
    tier: number;
};
/**
 * The technology parameters to make a new Technology.
 * @extends {{@link BaseParams `BaseParams`}
 */
type TechnologyParams = BaseParams & {
    /** The items needed to unlock this technology. */
    resourcesNeeded: InputOptions[];
    /** The items, factories, or builds that this technology unlocks. */
    unlocks: (Base | ItemCollector | BuildTierOptions)[];
    /**
     * The technologies that this technology unlocks.
     * @default []
     */
    unlocksTechnologies?: Technology[];
    /**
     * The technology needed to unlock this technology.
     * @default null
     */
    technologyNeeded?: Technology | null;
};
/**
 * This class helps you make a new Technology.
 * @extends {{@link BaseParams `BaseOptions`}
 */
declare class Technology extends Base {
    /**
     * All the technologies that has been created
     * @readonly
     */
    static readonly technologies: Technology[];
    /**
     * The image path of the Tech-tree.
     * @readonly
     */
    static get treeImage(): string;
    /**
     * Total price of all technologies
     * @readonly
     */
    static get totalPrice(): PriceParams;
    /**
     * Get a {@link Technology `Technology`} object.
     * @param name A technology name.
     * @returns An array of {@link Technology `Technology`} objects if found, else `undefined`.
     */
    static getTechnologiesByName(name: string): Technology[];
    /** The items needed to unlock this technology */
    resourcesNeeded: TechnologyParams["resourcesNeeded"];
    /** The items, factories, or builds that this technology unlocks. */
    unlocks: TechnologyParams["unlocks"];
    /**
     * The technologies that this technology unlocks.
     * @default []
    */
    unlocksTechnologies: NonNullable<TechnologyParams["unlocksTechnologies"]>;
    /**
     * The technology needed to unlock this technology.
     * @default null
     */
    technologyNeeded: NonNullable<TechnologyParams["technologyNeeded"]> | null;
    /**
     * Get the total price to get to this technology.
     * @readonly
     */
    get totalPrice(): Price;
    /**
     * Constructs a {@link Technology `Technology`} object.
     * @param params The technology parameters.
     * @param passByReference Whether to pass the objects in the {@link technology `technology`} by reference or not. Default is `true`.
     */
    constructor(params: TechnologyParams, passByReference?: boolean);
    /**
     * Constructs a {@link Technology `Technology`} object.
     * @param technology A {@link Technology `Technology`} object.
     * @param passByReference Whether to pass the objects in the {@link technology `technology`} by reference or not. Default is `true`.
     */
    constructor(technology: Technology, passByReference?: boolean);
    /**
     * Constructs a {@link Technology `Technology`} object.
     * @param technology A {@link Technology `Technology`} object or technology parameters.
     * @param passByReference Whether to pass the objects in the {@link technology `technology`} by reference or not. Default is `true`.
     */
    constructor(technology: Technology | TechnologyParams, passByReference?: boolean);
    /**
     * Check if a {@link Technology `Technology`} object is needed to unlock this {@link Technology `Technology`} object.
     * @param technology The {@link Technology `Technology`} object to check if it is needed to unlock this {@link Technology `Technology`} object.
     * @returns `true` if {@link technology `technology`} is needed, `false` otherwise.
     */
    private needsFunction;
    /**
     * Check if a {@link Technology `Technology`} object is needed to unlock this {@link Technology `Technology`} object.
     * @param technology The {@link Technology `Technology`} object to check if it is needed to unlock this {@link Technology `Technology`} object.
     * @returns `true` if {@link technology `technology`} is needed, `false` otherwise.
     */
    needs(technology: Technology): boolean;
    /**
     * Check if a {@link Technology `Technology`} object is needed to unlock this {@link Technology `Technology`} object.
     * @param name The technology name to check if it is needed to unlock this {@link Technology `Technology`} object.
     * @returns `true` if {@link name `technology`} is needed, `false` otherwise.
     */
    needs(name: string): boolean;
    /**
     * Check if a {@link Technology `Technology`} object is needed to unlock this {@link Technology `Technology`} object.
     * @param technology The {@link Technology `Technology`} object or technology name to check if it is needed to unlock this {@link Technology `Technology`} object.
     * @returns `true` if {@link technology `technology`} is needed, `false` otherwise.
     */
    needs(technology: Technology | string): boolean;
    /**
     * Determine if this technology and another technology are the equal
     * @param technology The other technology
     * @returns true if both technologies are the equal, false otherwise
     */
    equals(technology: Technology): boolean;
    /**
     * Determine if this technology and another technology are the strictly equal
     * @param technology The other technology
     * @returns true if both technologies are the strictly equal, false otherwise
     */
    strictlyEquals(technology: Technology): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for {@link Decoration `Decoration`}.
 * @extends {{@link BaseParams `BaseParams`}
 */
type DecorationParams = BaseParams & {};
/**
 * Create a new {@link Decoration `Decoration`}.
 * @extends {{@link Base `Base`}
 */
declare class Decoration extends Base {
    /**
     * All the decorations that has been created.
     * @readonly
    */
    static readonly decorations: Decoration[];
    /**
     * The description of all decoration builds.
     * @readonly
     */
    static get description(): string;
    /**
     * Constructs a new {@link Decoration `Decoration`} object.
     * @param params The decoration parameters.
     */
    constructor(params: DecorationParams);
    /**
     * Constructs a new {@link Decoration `Decoration`} object.
     * @param decoration A {@link Decoration `Decoration`} object.
     */
    constructor(decoration: Decoration);
}

/**
 * @author Space.yg
 */

/**
 * Parameters for {@link PowerPlant `PowerPlant`}.
 * @extends {{@link BaseParams `BaseParams`}
 */
type PowerPlantParams = BaseParams & {
    /** The input needed to activate the power plant. */
    input?: InputItemPerMin;
    /** The boost speed that the power plant gives. */
    speed: number;
    /** The amount of seconds the power plant is active before needing to recharge. */
    duration: number;
    /** The region of the power plant boost. */
    region: AreaParams | Area;
};
/**
 * Create a power plant.
 * @extends {{@link Base `Base`}
 */
declare class PowerPlant extends Base {
    /**
     * All the power plants that has been created.
     * @readonly
     */
    static readonly powerPlants: PowerPlant[];
    /**
     * The description of all power plants.
     * @readonly
     */
    static get description(): string;
    /** The input needed to activate the power plant. */
    input: PowerPlantParams["input"];
    /** The boost speed that the power plant gives. */
    speed: PowerPlantParams["speed"];
    /** The amount of seconds the power plant is active before needing to recharge. */
    duration: PowerPlantParams["duration"];
    /** The region of the power plant boost. */
    region: Area;
    /**
     * Constructs a new {@link PowerPlant `PowerPlant`} object.
     * @param params The power plant parameters.
     * @param passByReference Whether to pass the objects in {@link params `params`} by reference or not. Default is `true`.
     */
    constructor(params: PowerPlantParams, passByReference?: boolean);
    /**s
     * Constructs a new {@link PowerPlant `PowerPlant`} object.
     * @param powerPlant A {@link PowerPlant `PowerPlant`} object.
     * @param passByReference Whether to pass the objects in {@link powerPlant `powerPlant`} by reference or not. Default is `true`.
     */
    constructor(powerPlant: PowerPlant, passByReference?: boolean);
    /**
     * Constructs a new {@link PowerPlant `PowerPlant`} object.
     * @param powerPlant A {@link PowerPlant `PowerPlant`} object or power plant parameters.
     * @param passByReference Whether to pass the objects in {@link powerPlant `powerPlant`} by reference or not. Default is `true`.
     */
    constructor(powerPlant: PowerPlant | PowerPlantParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param powerPlant The other {@link PowerPlant `PowerPlant`} object.
     * @returns `true` if both {@link PowerPlant `PowerPlant`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(powerPlant: PowerPlant): boolean;
    /**
     * Determine if this {@link PowerPlant `PowerPlant`} object is equal to another {@link PowerPlant `PowerPlant`} object.
     * @param powerPlant The other {@link PowerPlant `PowerPlant`} object.
     * @returns `true` if both {@link PowerPlant `PowerPlant`} objects are equal, `false` otherwise.
     */
    equals(powerPlant: PowerPlant): boolean;
    /**
     * Determine if this {@link PowerPlant `PowerPlant`} object is strictly equal to another {@link PowerPlant `PowerPlant`} object.
     * @param powerPlant The other {@link PowerPlant `PowerPlant`} object.
     * @returns `true` if both {@link PowerPlant `PowerPlant`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(powerPlant: PowerPlant): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for a distance tier.
 * @extends {{@link TierParams `TierParams`}
 */
type DistanceTierParams = TierParams & {
    /** The distance at this tier. */
    distance: number;
};
/**
 * A distance tier.
 * @extends {{@link Tier `Tier`}
 */
declare class DistanceTier extends Tier {
    /** The output at this tier. */
    distance: DistanceTierParams["distance"];
    /**
     * Constructs a new {@link DistanceTier `DistanceTier`} object.
     * @param params The distance tier parameters.
     */
    constructor(params: DistanceTierParams);
    /**
     * Constructs a new {@link DistanceTier `DistanceTier`} object.
     * @param distanceTier A {@link DistanceTier `DistanceTier`} object.
     */
    constructor(distanceTier: DistanceTier);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param distanceTier The other {@link DistanceTier `DistanceTier`} object.
     * @returns `true` if both {@link DistanceTier `DistanceTier`} objects are the equal in the properties that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(distanceTier: DistanceTier): boolean;
    /**
     * Determine if this {@link DistanceTier `DistanceTier`} object is equal to another {@link DistanceTier `DistanceTier`} object.
     * @param distanceTier The other {@link DistanceTier `DistanceTier`} object.
     * @returns `true` if both {@link DistanceTier `DistanceTier`} objects are equal, `false` otherwise.
     */
    equals(distanceTier: DistanceTier): boolean;
    /**
     * Determine if this {@link DistanceTier `DistanceTier`} object is strictly equal to another {@link DistanceTier `DistanceTier`} object.
     * @param distanceTier The other {@link DistanceTier `DistanceTier`} object.
     * @returns `true` if both {@link DistanceTier `DistanceTier`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(distanceTier: DistanceTier): boolean;
}

/**
 * @author Space.yg
 */

/**
 * The parameters of the optional distance tiers.
 * @extends {{@link TiersParams `TiersParams`}
 */
type DistanceTiersParams = TiersParams<DistanceTierParams, DistanceTier>;
/**
 * Make a new {@link DistanceTier `StorageTier`}.
 * @extends {{@link Tiers `Tiers`}
 */
declare class DistanceTiers extends Tiers {
    tiers: {
        [/** The tiers. They must be consecutive integers */ tier: number]: DistanceTier;
    };
    /**
     * Constructs a new {@link DistanceTiers `DistanceTiers`} object.
     * @param params The tier parameters.
     * @param passByReference Whether to pass the objects in the {@link params `params`} by reference or not. Default is `true`.
     */
    constructor(params: DistanceTiersParams, passByReference?: boolean);
    /**
     * Constructs a new {@link DistanceTiers `DistanceTiers`} object.
     * @param distanceTier A {@link DistanceTiers `DistanceTiers`} object.
     * @param passByReference Whether to pass the objects in the {@link DistanceTiers `DistanceTiers`} by reference or not. Default is `true`.
     */
    constructor(distanceTier: DistanceTiers, passByReference?: boolean);
    /**
     * Constructs a new {@link DistanceTiers `DistanceTiers`} object.
     * @param distanceTier A {@link DistanceTiers `DistanceTiers`} object or tier parameters.
     * @param passByReference Whether to pass the objects in the {@link DistanceTiers `DistanceTiers`} by reference or not. Default is `true`.
     */
    constructor(distanceTier: DistanceTiers | DistanceTiersParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param distanceTier The other {@link DistanceTiers `DistanceTiers`} object.
     * @returns `true` if both {@link DistanceTiers `DistanceTiers`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(distanceTier: DistanceTiers): boolean;
    /**
     * Determine if this {@link DistanceTiers `DistanceTiers`} object is equal to another {@link DistanceTiers `DistanceTiers`} object.
     * @param distanceTier The other {@link DistanceTiers `DistanceTiers`} object.
     * @returns `true` if both {@link DistanceTiers `DistanceTiers`} objects are equal, `false` otherwise.
     */
    equals(distanceTier: DistanceTiers): boolean;
    /**
     * Determine if this {@link DistanceTiers `DistanceTiers`} object is strictly equal to another {@link DistanceTiers `DistanceTiers`} object.
     * @param distanceTier The other {@link DistanceTiers `DistanceTiers`} object.
     * @returns `true` if both {@link DistanceTiers `DistanceTiers`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(distanceTier: DistanceTiers): boolean;
}

/**
 * @author Space.yg
 */

/** Parameters for {@link TransportationDistance `TransportationDistance`}. */
type TransportationDistanceParams = {
    /** The name of the transportation distance. */
    name: string;
    /** The description of the transportation distance. This is the in-game description. */
    description: string;
    /** The tiers of the transportation distance. */
    tiers: DistanceTiersParams | DistanceTiers;
};
/**
 * Make a new transportation distance.
 * @extends {{@link DistanceTiers `DistanceTiers`}
 */
declare class TransportationDistance extends DistanceTiers {
    /**
     * All the transportation distances that has been created.
     * @readonly
     */
    static readonly transportationDistances: TransportationDistance[];
    /** The name of the transportation distance. */
    name: TransportationDistanceParams["name"];
    /** The price of the first tier of the transportation distance. */
    price: Price;
    /** The description of the transportation distance. */
    description: TransportationDistanceParams["description"];
    /**
     * Constructs a new {@link TransportationDistance `TransportationDistance`} object.
     * @param params The transportation distance parameters.
     * @param passByReference Whether to pass the objects in the {@link TransportationDistance `TransportationDistance`} by reference or not. Default is `true`.
     */
    constructor(params: TransportationDistanceParams, passByReference?: boolean);
    /**
     * Constructs a new {@link TransportationDistance `TransportationDistance`} object.
     * @param transportationDistance A {@link TransportationDistance `TransportationDistance`} object.
     * @param passByReference Whether to pass the objects in the {@link TransportationDistance `TransportationDistance`} by reference or not. Default is `true`.
     */
    constructor(transportationDistance: TransportationDistance, passByReference?: boolean);
    /**
     * Constructs a new {@link TransportationDistance `TransportationDistance`} object.
     * @param transportationDistance A {@link TransportationDistance `TransportationDistance`} object or transportation distance parameters.
     * @param passByReference Whether to pass the objects in the {@link TransportationDistance `TransportationDistance`} by reference or not. Default is `true`.
     */
    constructor(transportationDistance: TransportationDistance | TransportationDistanceParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param transportationDistance The other {@link TransportationDistance `TransportationDistance`} object.
     * @returns `true` if both {@link TransportationDistance `TransportationDistance`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(transportationDistance: TransportationDistance): boolean;
    /**
     * Determine if this {@link TransportationDistance `TransportationDistance`} object is equal to another {@link TransportationDistance `TransportationDistance`} object.
     * @param transportationDistance The other {@link TransportationDistance `TransportationDistance`} object.
     * @returns `true` if both storages are the equal, `false` otherwise.
     */
    equals(transportationDistance: TransportationDistance): boolean;
    /**
     * Determine if this {@link TransportationDistance `TransportationDistance`} object is strictly equal to another {@link TransportationDistance `TransportationDistance`} object.
     * @param transportationDistance The other {@link TransportationDistance `TransportationDistance`} object.
     * @returns `true` if both storages are the strictly equal, `false` otherwise.
     */
    strictlyEquals(transportationDistance: TransportationDistance): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for a speed tier.
 * @extends {{@link TierParams `TierParams`}
 */
type SpeedTierParams = TierParams & {
    /** The speed at this tier. */
    speed: number;
};
/**
 * A speed tier.
 * @extends {{@link Tier `Tier`}
 */
declare class SpeedTier extends Tier {
    /** The speed at this tier. */
    speed: SpeedTierParams["speed"];
    /**
     * Constructs a new {@link SpeedTier `SpeedTier`} object.
     * @param params The speed tier parameters.
     */
    constructor(params: SpeedTierParams);
    /**
     * Constructs a new {@link SpeedTier `SpeedTier`} object.
     * @param speedTier A {@link SpeedTier `SpeedTier`} object.
     */
    constructor(speedTier: SpeedTier);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param speedTier The other {@link SpeedTier `SpeedTier`} object.
     * @returns `true` if both {@link SpeedTier `SpeedTier`} objects are the equal in the properties that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(speedTier: SpeedTier): boolean;
    /**
     * Determine if this {@link SpeedTier `SpeedTier`} object is equal to another {@link SpeedTier `SpeedTier`} object.
     * @param speedTier The other {@link SpeedTier `SpeedTier`} object.
     * @returns `true` if both {@link SpeedTier `SpeedTier`} objects are equal, `false` otherwise.
     */
    equals(speedTier: SpeedTier): boolean;
    /**
     * Determine if this {@link SpeedTier `SpeedTier`} object is strictly equal to another {@link SpeedTier `SpeedTier`} object.
     * @param speedTier The other {@link SpeedTier `SpeedTier`} object.
     * @returns `true` if both {@link SpeedTier `SpeedTier`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(speedTier: SpeedTier): boolean;
}

/**
 * @author Space.yg
 */

/**
 * The parameters of the optional speed tiers.
 * @extends {{@link TiersParams `TiersParams`}
 */
type SpeedTiersParams = TiersParams<SpeedTierParams, SpeedTier>;
/**
 * Make a new speed tiers.
 * @extends {{@link Tiers `Tiers`}
 */
declare class SpeedTiers extends Tiers {
    tiers: {
        [/** The tiers. They must be consecutive integers. */ tier: number]: SpeedTier;
    };
    get maxTier(): SpeedTier;
    get minTier(): SpeedTier;
    /**
     * Constructs a new {@link SpeedTiers `StorageTiers`} object.
     * @param params The tier parameters.
     * @param passByReference Whether to pass the objects in the {@link params `params`} by reference or not. Default is `true`.
     */
    constructor(params: SpeedTiersParams, passByReference?: boolean);
    /**
     * Constructs a new {@link SpeedTiers `StorageTiers`} object.
     * @param speedTiers A {@link SpeedTiers `StorageTiers`} object.
     * @param passByReference Whether to pass the objects in the {@link SpeedTiers `StorageTiers`} by reference or not. Default is `true`.
     */
    constructor(speedTiers: SpeedTiers, passByReference?: boolean);
    /**
     * Constructs a new {@link SpeedTiers `StorageTiers`} object.
     * @param speedTiers A {@link SpeedTiers `StorageTiers`} object or tier parameters.
     * @param passByReference Whether to pass the objects in the {@link SpeedTiers `StorageTiers`} by reference or not. Default is `true`.
     */
    constructor(speedTiers: SpeedTiers | SpeedTiersParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param speedTiers The other {@link SpeedTiers `StorageTiers`} object.
     * @returns `true` if both {@link SpeedTiers `StorageTiers`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(speedTiers: SpeedTiers): boolean;
    /**
     * Determine if this {@link SpeedTiers `StorageTiers`} object is equal to another {@link SpeedTiers `StorageTiers`} object.
     * @param speedTiers The other {@link SpeedTiers `StorageTiers`} object.
     * @returns `true` if both {@link SpeedTiers `StorageTiers`} objects are equal, `false` otherwise.
     */
    equals(speedTiers: SpeedTiers): boolean;
    /**
     * Determine if this {@link SpeedTiers `StorageTiers`} object is strictly equal to another {@link SpeedTiers `StorageTiers`} object.
     * @param speedTiers The other {@link SpeedTiers `StorageTiers`} object.
     * @returns `true` if both {@link SpeedTiers `StorageTiers`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(speedTiers: SpeedTiers): boolean;
}

/**
 * @author Space.yg
 */

/** Parameters for {@link TransportationSpeed `TransportationSpeed`}. */
type TransportationSpeedParams = {
    /** The name of the transportation speed. */
    name: string;
    /** The description of the transportation speed. */
    description: string;
    /** The tiers of the transportation speed. */
    tiers: SpeedTiersParams | SpeedTiers;
};
/**
 * Create a new transportation speed.
 * @extends {{@link SpeedTiers `SpeedTiers`}
 */
declare class TransportationSpeed extends SpeedTiers {
    /**
     * All the transportations that has been created.
     * @readonly
     */
    static readonly transportationSpeeds: TransportationSpeed[];
    /** The name of the transportation. */
    name: TransportationSpeedParams["name"];
    /** The price of the first tier of the transportation. */
    price: Price;
    /** The description of the transportation. */
    description: TransportationSpeedParams["description"];
    /**
     * Constructs a new {@link TransportationSpeed `TransportationSpeed`} object.
     * @param params The transportation parameters
     * @param passByReference Whether to pass the objects in the {@link TransportationSpeed `TransportationSpeed`} by reference or not. Default is `true`.
     */
    constructor(params: TransportationSpeedParams, passByReference?: boolean);
    /**
     * Constructs a new {@link TransportationSpeed `TransportationSpeed`} object.
     * @param transportation A {@link TransportationSpeed `TransportationSpeed`} object.
     * @param passByReference Whether to pass the objects in the {@link TransportationSpeed `TransportationSpeed`} by reference or not. Default is `true`.
     */
    constructor(transportation: TransportationSpeed, passByReference?: boolean);
    /**
     * Constructs a new {@link TransportationSpeed `TransportationSpeed`} object.
     * @param transportation A {@link TransportationSpeed `TransportationSpeed`} object or transportation parameters.
     * @param passByReference Whether to pass the objects in the {@link TransportationSpeed `TransportationSpeed`} by reference or not. Default is `true`.
     */
    constructor(transportation: TransportationSpeed | TransportationSpeedParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param transportationSpeed The other {@link TransportationSpeed `TransportationSpeed`} object.
     * @returns `true` if both {@link TransportationSpeed `TransportationSpeed`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(transportationSpeed: TransportationSpeed): boolean;
    /**
     * Determine if this {@link TransportationSpeed `TransportationSpeed`} object is equal to another {@link TransportationSpeed `TransportationSpeed`} object.
     * @param transportationSpeed The other {@link TransportationSpeed `TransportationSpeed`} object.
     * @returns `true` if both storages are the equal, `false` otherwise.
     */
    equals(transportationSpeed: TransportationSpeed): boolean;
    /**
     * Determine if this {@link TransportationSpeed `TransportationSpeed`} object is strictly equal to another {@link TransportationSpeed `TransportationSpeed`} object.
     * @param transportationSpeed speed The other {@link TransportationSpeed `TransportationSpeed`} object.
     * @returns `true` if both storages are the strictly equal, `false` otherwise.
     */
    strictlyEquals(transportationSpeed: TransportationSpeed): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for a storage tier.
 * @extends {{@link TierParams `TierParams`}
 */
type StorageTierParams = TierParams & {
    /** The storage at this tier. */
    storage: number;
};
/**
 * A distance tier.
 * @extends {{@link Tier `Tier`}
 */
declare class StorageTier extends Tier {
    /** The output at this tier. */
    storage: StorageTierParams["storage"];
    /**
     * Constructs a new {@link StorageTier `StorageTier`} object.
     * @param params The storage tier parameters.
     */
    constructor(params: StorageTierParams);
    /**
     * Constructs a new {@link StorageTier `StorageTier`} object.
     * @param storageTier A {@link StorageTier `StorageTier`} object.
     */
    constructor(storageTier: StorageTier);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param distanceTier The other {@link StorageTier `StorageTier`} object.
     * @returns `true` if both {@link StorageTier `StorageTier`} objects are the equal in the properties that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(storageTier: StorageTier): boolean;
    /**
     * Determine if this {@link StorageTier `StorageTier`} object is equal to another {@link StorageTier `StorageTier`} object.
     * @param storageTier The other {@link StorageTier `StorageTier`} object.
     * @returns `true` if both {@link StorageTier `StorageTier`} objects are equal, `false` otherwise.
     */
    equals(storageTier: StorageTier): boolean;
    /**
     * Determine if this {@link StorageTier `StorageTier`} object is strictly equal to another {@link StorageTier `StorageTier`} object.
     * @param storageTier The other {@link StorageTier `StorageTier`} object.
     * @returns `true` if both {@link StorageTier `StorageTier`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(storageTier: StorageTier): boolean;
}

/**
 * @author Space.yg
 */

/**
 * The parameters of the optional storage tiers.
 * @extends {{@link TiersParams `TiersParams`}
 */
type StorageTiersParams = TiersParams<StorageTierParams, StorageTier>;
/**
 * Make a new {@link StorageTier `StorageTier`}.
 * @extends {{@link Tiers `Tiers`}
 */
declare class StorageTiers extends Tiers {
    tiers: {
        [/** The tiers. They must be consecutive integers. */ tier: number]: StorageTier;
    };
    get maxTier(): StorageTier;
    get minTier(): StorageTier;
    /**
     * Constructs a new {@link StorageTiers `StorageTiers`} object.
     * @param params The tier parameters.
     * @param passByReference Whether to pass the objects in the {@link params `params`} by reference or not. Default is `true`.
     */
    constructor(params: StorageTiersParams, passByReference?: boolean);
    /**
     * Constructs a new {@link StorageTiers `StorageTiers`} object.
     * @param storageTiers A {@link StorageTiers `StorageTiers`} object.
     * @param passByReference Whether to pass the objects in the {@link StorageTiers `StorageTiers`} by reference or not. Default is `true`.
     */
    constructor(storageTiers: StorageTiers, passByReference?: boolean);
    /**
     * Constructs a new {@link StorageTiers `StorageTiers`} object.
     * @param storageTiers A {@link StorageTiers `StorageTiers`} object or tier parameters.
     * @param passByReference Whether to pass the objects in the {@link StorageTiers `StorageTiers`} by reference or not. Default is `true`.
     */
    constructor(storageTiers: StorageTiers | StorageTiersParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param storageTiers The other {@link StorageTiers `StorageTiers`} object.
     * @returns `true` if both {@link StorageTiers `StorageTiers`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(storageTiers: StorageTiers): boolean;
    /**
     * Determine if this {@link StorageTiers `StorageTiers`} object is equal to another {@link StorageTiers `StorageTiers`} object.
     * @param storageTiers The other {@link StorageTiers `StorageTiers`} object.
     * @returns `true` if both {@link StorageTiers `StorageTiers`} objects are equal, `false` otherwise.
     */
    equals(storageTiers: StorageTiers): boolean;
    /**
     * Determine if this {@link StorageTiers `StorageTiers`} object is strictly equal to another {@link StorageTiers `StorageTiers`} object.
     * @param storageTiers The other {@link StorageTiers `StorageTiers`} object.
     * @returns `true` if both {@link StorageTiers `StorageTiers`} objects are strictly equal, `false` otherwise.
     */
    strictlyEquals(storageTiers: StorageTiers): boolean;
}

/**
 * @author Space.yg
 */

/** Parameters for {@link Storage `Storage`}. */
type StorageParams = {
    /** The name of the storage. */
    name: string;
    /** The description of the storage. */
    description: string;
    /** The tiers of the storage. */
    tiers: StorageTiersParams | StorageTiers;
};
/**
 * Create a new storage.
 * @extends {{@link StorageTiers `StorageTiers`}
 */
declare class Storage extends StorageTiers {
    /**
     * All the transportations that has been created.
     * @readonly
     */
    static readonly storages: Storage[];
    /** The name of the storage. */
    name: StorageParams["name"];
    /**
     * The price of the storage.
     * @default
     * ```javascript
     * new Price({
     * 	gold: 0,
     * 	gems: 0
     * })
     * ```
     */
    price: Price;
    /** The description of the storage. */
    description: StorageParams["description"];
    /**
     * Construct a {@link Storage `Storage`} object.
     * @param params The tier parameters.
     */
    constructor(params: StorageParams, passByReference?: boolean);
    /**
     * Construct a {@link Storage `Storage`} object.
     * @param storage A {@link Storage `Storage`} object.
     */
    constructor(storage: Storage, passByReference?: boolean);
    /**
     * Construct a {@link Storage `Storage`} object.
     * @param storage A {@link Storage `Storage`} object or tier parameters.
     */
    constructor(storage: Storage | StorageParams, passByReference?: boolean);
    /**
     * These are similarities between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods.
     * @param storage The other {@link Storage `Storage`} object.
     * @returns `true` if both {@link Storage `Storage`} objects are the equal in the things that are similar between the {@link equals `equals`} and {@link strictlyEquals `strictlyEquals`} methods, `false` otherwise.
     */
    protected similarEquals(storage: Storage): boolean;
    /**
     * Determine if this {@link Storage `Storage`} object is equal to another {@link Storage `Storage`} object.
     * @param storage The other {@link Storage `Storage`} object.
     * @returns `true` if both storages are the equal, `false` otherwise.
     */
    equals(storage: Storage): boolean;
    /**
     * Determine if this {@link Storage `Storage`} object is strictly equal to {@link Storage `Storage`} object.
     * @param storage The other {@link Storage `Storage`} object.
     * @returns `true` if both storages are strictly equal, `false` otherwise.
     */
    strictlyEquals(storage: Storage): boolean;
}

/**
 * @author Space.yg
 */

/**
 * Parameters for {@link Currency `Currency`}.
 * @extends {{@link BaseParams `BaseParams`}
 */
type CurrencyParams = BaseParams & {};
/** Create a new currency */
declare class Currency extends Base {
    /**
     * All the currencies that has been created.
     * @readonly
    */
    static readonly currencies: Currency[];
    /**
     * Constructs a new {@link Currency `Currency`} object.
     * @param params The currency parameters.
     */
    constructor(params: CurrencyParams);
    /**
     * Constructs a new {@link Currency `Currency`} object.
     * @param currency A {@link Currency `Currency`} object.
     */
    constructor(currency: Currency);
}

/**
 * @author Space.yg
 */

/** Gold */
declare const gold: Currency;
/** Gem */
declare const gem: Currency;

declare const currencies_gem: typeof gem;
declare const currencies_gold: typeof gold;
declare namespace currencies {
  export { currencies_gem as gem, currencies_gold as gold };
}

/**
 * @author Space.yg
 */

declare const woodLog: Item;
declare const stone: Item;
declare const ironOre: Item;
declare const copperOre: Item;
declare const coal: Item;
declare const wolframite: Item;
declare const woodPlank: Item;
declare const sand$1: Item;
declare const ironIngot: Item;
declare const copperIngot: Item;
declare const tungstenOre: Item;
declare const graphite: Item;
declare const woodFrame$1: Item;
declare const copperWire: Item;
declare const heatSink: Item;
declare const ironGear: Item;
declare const ironPlating: Item;
declare const steel: Item;
declare const steelRod: Item;
declare const glass: Item;
declare const condenserLens: Item;
declare const carbonFiber: Item;
declare const tungstenCarbide: Item;
declare const coupler: Item;
declare const concrete$1: Item;
declare const metalFrame: Item;
declare const industrialFrame$1: Item;
declare const silicon: Item;
declare const logicCircuit: Item;
declare const nanoWire$1: Item;
declare const turbocharger$1: Item;
declare const electromagnet: Item;
declare const battery: Item;
declare const rotor: Item;
declare const electricMotor: Item;
declare const tank: Item;
declare const matterCompressor$1: Item;
declare const particleGlue: Item;
declare const gyroscope: Item;
declare const energyCube$1: Item;
declare const computer: Item;
declare const stabilizer$1: Item;
declare const magneticFieldGenerator: Item;
declare const quantumEntangler: Item;
declare const electronMicroscope: Item;
declare const superComputer: Item;
declare const atomicLocator$1: Item;
declare const matterDuplicator: Item;
declare const earthToken: Item;
declare const uraniumOre: Item;
declare const enrichedUranium: Item;
declare const emptyFuelCell: Item;
declare const nuclearFuelCell: Item;
declare const gemApple: Item;

declare const items_battery: typeof battery;
declare const items_carbonFiber: typeof carbonFiber;
declare const items_coal: typeof coal;
declare const items_computer: typeof computer;
declare const items_condenserLens: typeof condenserLens;
declare const items_copperIngot: typeof copperIngot;
declare const items_copperOre: typeof copperOre;
declare const items_copperWire: typeof copperWire;
declare const items_coupler: typeof coupler;
declare const items_earthToken: typeof earthToken;
declare const items_electricMotor: typeof electricMotor;
declare const items_electromagnet: typeof electromagnet;
declare const items_electronMicroscope: typeof electronMicroscope;
declare const items_emptyFuelCell: typeof emptyFuelCell;
declare const items_enrichedUranium: typeof enrichedUranium;
declare const items_gemApple: typeof gemApple;
declare const items_glass: typeof glass;
declare const items_graphite: typeof graphite;
declare const items_gyroscope: typeof gyroscope;
declare const items_heatSink: typeof heatSink;
declare const items_ironGear: typeof ironGear;
declare const items_ironIngot: typeof ironIngot;
declare const items_ironOre: typeof ironOre;
declare const items_ironPlating: typeof ironPlating;
declare const items_logicCircuit: typeof logicCircuit;
declare const items_magneticFieldGenerator: typeof magneticFieldGenerator;
declare const items_matterDuplicator: typeof matterDuplicator;
declare const items_metalFrame: typeof metalFrame;
declare const items_nuclearFuelCell: typeof nuclearFuelCell;
declare const items_particleGlue: typeof particleGlue;
declare const items_quantumEntangler: typeof quantumEntangler;
declare const items_rotor: typeof rotor;
declare const items_silicon: typeof silicon;
declare const items_steel: typeof steel;
declare const items_steelRod: typeof steelRod;
declare const items_stone: typeof stone;
declare const items_superComputer: typeof superComputer;
declare const items_tank: typeof tank;
declare const items_tungstenCarbide: typeof tungstenCarbide;
declare const items_tungstenOre: typeof tungstenOre;
declare const items_uraniumOre: typeof uraniumOre;
declare const items_wolframite: typeof wolframite;
declare const items_woodLog: typeof woodLog;
declare const items_woodPlank: typeof woodPlank;
declare namespace items {
  export { atomicLocator$1 as atomicLocator, items_battery as battery, items_carbonFiber as carbonFiber, items_coal as coal, items_computer as computer, concrete$1 as concrete, items_condenserLens as condenserLens, items_copperIngot as copperIngot, items_copperOre as copperOre, items_copperWire as copperWire, items_coupler as coupler, items_earthToken as earthToken, items_electricMotor as electricMotor, items_electromagnet as electromagnet, items_electronMicroscope as electronMicroscope, items_emptyFuelCell as emptyFuelCell, energyCube$1 as energyCube, items_enrichedUranium as enrichedUranium, items_gemApple as gemApple, items_glass as glass, items_graphite as graphite, items_gyroscope as gyroscope, items_heatSink as heatSink, industrialFrame$1 as industrialFrame, items_ironGear as ironGear, items_ironIngot as ironIngot, items_ironOre as ironOre, items_ironPlating as ironPlating, items_logicCircuit as logicCircuit, items_magneticFieldGenerator as magneticFieldGenerator, matterCompressor$1 as matterCompressor, items_matterDuplicator as matterDuplicator, items_metalFrame as metalFrame, nanoWire$1 as nanoWire, items_nuclearFuelCell as nuclearFuelCell, items_particleGlue as particleGlue, items_quantumEntangler as quantumEntangler, items_rotor as rotor, sand$1 as sand, items_silicon as silicon, stabilizer$1 as stabilizer, items_steel as steel, items_steelRod as steelRod, items_stone as stone, items_superComputer as superComputer, items_tank as tank, items_tungstenCarbide as tungstenCarbide, items_tungstenOre as tungstenOre, turbocharger$1 as turbocharger, items_uraniumOre as uraniumOre, items_wolframite as wolframite, woodFrame$1 as woodFrame, items_woodLog as woodLog, items_woodPlank as woodPlank };
}

/**
 * @author Space.yg
 */

declare const manufacturerUpgrade3: Technology;
declare const manufacturerUpgrade2: Technology;
declare const manufacturerUpgrade1: Technology;
declare const forgeUpgrade3: Technology;
declare const forgeUpgrade2: Technology;
declare const forgeUpgrade1: Technology;
declare const industrialFactoryUpgrade3: Technology;
declare const industrialFactoryUpgrade2: Technology;
declare const industrialFactoryUpgrade1: Technology;
declare const gemTree$1: Technology;
declare const earthStatue$1: Technology;
declare const particleStatue$1: Technology;
declare const steelStatue$1: Technology;
declare const circuitStatue$1: Technology;
declare const gearStatue$1: Technology;
declare const decorativeWalls: Technology;
declare const factoryDecorations: Technology;
declare const decorativeRocks: Technology;
declare const festiveDecorations: Technology;
declare const decorativeTrees: Technology;
declare const roboticArmUpgrade3: Technology;
declare const roboticArmUpgrade2: Technology;
declare const roboticArmUpgrade1: Technology;
declare const robotics: Technology;
declare const machineShopUpgrade3: Technology;
declare const machineShopUpgrade2: Technology;
declare const machineShopUpgrade1: Technology;
declare const storageUpgrade3: Technology;
declare const storageUpgrade2: Technology;
declare const storageUpgrade1: Technology;
declare const storage$1: Technology;
declare const furnaceUpgrade3: Technology;
declare const furnaceUpgrade2: Technology;
declare const furnaceUpgrade1: Technology;
declare const nuclearPower: Technology;
declare const coalPower: Technology;
declare const workshopUpgrade3: Technology;
declare const workshopUpgrade2: Technology;
declare const workshopUpgrade1: Technology;
declare const undergroundBeltUpgrade2: Technology;
declare const undergroundBeltUpgrade1: Technology;
declare const speedUpgradeXII: Technology;
declare const speedUpgradeXI: Technology;
declare const speedUpgradeX: Technology;
declare const speedUpgradeIX: Technology;
declare const speedUpgradeVIII: Technology;
declare const speedUpgradeVII: Technology;
declare const speedUpgradeVI: Technology;
declare const speedUpgradeV: Technology;
declare const speedUpgradeIV: Technology;
declare const speedUpgradeIII: Technology;
declare const speedUpgradeII: Technology;
declare const speedUpgradeI: Technology;
declare const advancedBelts: Technology;
declare const uraniumProcessing: Technology;
declare const extractorUpgrade4: Technology;
declare const extractorUpgrade3: Technology;
declare const extractorUpgrade2: Technology;
declare const extractorUpgrade1: Technology;
declare const earthTeleporter$1: Technology;
declare const matterDuplication: Technology;
declare const quantumEntanglement: Technology;
declare const atomicLocator: Technology;
declare const energyCube: Technology;
declare const matterCompressor: Technology;
declare const industrialFrame: Technology;
declare const stabilizer: Technology;
declare const superComputers: Technology;
declare const turbocharger: Technology;
declare const tungstenProduction: Technology;
declare const microscopes: Technology;
declare const nanoWire: Technology;
declare const motors: Technology;
declare const batteries: Technology;
declare const concrete: Technology;
declare const steelProduction: Technology;
declare const goldVault$1: Technology;
declare const computers: Technology;
declare const circuitry: Technology;
declare const sand: Technology;
declare const metalFrames: Technology;
declare const magnets: Technology;
declare const metalWorking: Technology;
declare const smelting: Technology;
declare const woodFrame: Technology;
declare const woodPlanks: Technology;
declare const startingTech: Technology;

declare const technologies_advancedBelts: typeof advancedBelts;
declare const technologies_atomicLocator: typeof atomicLocator;
declare const technologies_batteries: typeof batteries;
declare const technologies_circuitry: typeof circuitry;
declare const technologies_coalPower: typeof coalPower;
declare const technologies_computers: typeof computers;
declare const technologies_concrete: typeof concrete;
declare const technologies_decorativeRocks: typeof decorativeRocks;
declare const technologies_decorativeTrees: typeof decorativeTrees;
declare const technologies_decorativeWalls: typeof decorativeWalls;
declare const technologies_energyCube: typeof energyCube;
declare const technologies_extractorUpgrade1: typeof extractorUpgrade1;
declare const technologies_extractorUpgrade2: typeof extractorUpgrade2;
declare const technologies_extractorUpgrade3: typeof extractorUpgrade3;
declare const technologies_extractorUpgrade4: typeof extractorUpgrade4;
declare const technologies_factoryDecorations: typeof factoryDecorations;
declare const technologies_festiveDecorations: typeof festiveDecorations;
declare const technologies_forgeUpgrade1: typeof forgeUpgrade1;
declare const technologies_forgeUpgrade2: typeof forgeUpgrade2;
declare const technologies_forgeUpgrade3: typeof forgeUpgrade3;
declare const technologies_furnaceUpgrade1: typeof furnaceUpgrade1;
declare const technologies_furnaceUpgrade2: typeof furnaceUpgrade2;
declare const technologies_furnaceUpgrade3: typeof furnaceUpgrade3;
declare const technologies_industrialFactoryUpgrade1: typeof industrialFactoryUpgrade1;
declare const technologies_industrialFactoryUpgrade2: typeof industrialFactoryUpgrade2;
declare const technologies_industrialFactoryUpgrade3: typeof industrialFactoryUpgrade3;
declare const technologies_industrialFrame: typeof industrialFrame;
declare const technologies_machineShopUpgrade1: typeof machineShopUpgrade1;
declare const technologies_machineShopUpgrade2: typeof machineShopUpgrade2;
declare const technologies_machineShopUpgrade3: typeof machineShopUpgrade3;
declare const technologies_magnets: typeof magnets;
declare const technologies_manufacturerUpgrade1: typeof manufacturerUpgrade1;
declare const technologies_manufacturerUpgrade2: typeof manufacturerUpgrade2;
declare const technologies_manufacturerUpgrade3: typeof manufacturerUpgrade3;
declare const technologies_matterCompressor: typeof matterCompressor;
declare const technologies_matterDuplication: typeof matterDuplication;
declare const technologies_metalFrames: typeof metalFrames;
declare const technologies_metalWorking: typeof metalWorking;
declare const technologies_microscopes: typeof microscopes;
declare const technologies_motors: typeof motors;
declare const technologies_nanoWire: typeof nanoWire;
declare const technologies_nuclearPower: typeof nuclearPower;
declare const technologies_quantumEntanglement: typeof quantumEntanglement;
declare const technologies_roboticArmUpgrade1: typeof roboticArmUpgrade1;
declare const technologies_roboticArmUpgrade2: typeof roboticArmUpgrade2;
declare const technologies_roboticArmUpgrade3: typeof roboticArmUpgrade3;
declare const technologies_robotics: typeof robotics;
declare const technologies_sand: typeof sand;
declare const technologies_smelting: typeof smelting;
declare const technologies_speedUpgradeI: typeof speedUpgradeI;
declare const technologies_speedUpgradeII: typeof speedUpgradeII;
declare const technologies_speedUpgradeIII: typeof speedUpgradeIII;
declare const technologies_speedUpgradeIV: typeof speedUpgradeIV;
declare const technologies_speedUpgradeIX: typeof speedUpgradeIX;
declare const technologies_speedUpgradeV: typeof speedUpgradeV;
declare const technologies_speedUpgradeVI: typeof speedUpgradeVI;
declare const technologies_speedUpgradeVII: typeof speedUpgradeVII;
declare const technologies_speedUpgradeVIII: typeof speedUpgradeVIII;
declare const technologies_speedUpgradeX: typeof speedUpgradeX;
declare const technologies_speedUpgradeXI: typeof speedUpgradeXI;
declare const technologies_speedUpgradeXII: typeof speedUpgradeXII;
declare const technologies_stabilizer: typeof stabilizer;
declare const technologies_startingTech: typeof startingTech;
declare const technologies_steelProduction: typeof steelProduction;
declare const technologies_storageUpgrade1: typeof storageUpgrade1;
declare const technologies_storageUpgrade2: typeof storageUpgrade2;
declare const technologies_storageUpgrade3: typeof storageUpgrade3;
declare const technologies_superComputers: typeof superComputers;
declare const technologies_tungstenProduction: typeof tungstenProduction;
declare const technologies_turbocharger: typeof turbocharger;
declare const technologies_undergroundBeltUpgrade1: typeof undergroundBeltUpgrade1;
declare const technologies_undergroundBeltUpgrade2: typeof undergroundBeltUpgrade2;
declare const technologies_uraniumProcessing: typeof uraniumProcessing;
declare const technologies_woodFrame: typeof woodFrame;
declare const technologies_woodPlanks: typeof woodPlanks;
declare const technologies_workshopUpgrade1: typeof workshopUpgrade1;
declare const technologies_workshopUpgrade2: typeof workshopUpgrade2;
declare const technologies_workshopUpgrade3: typeof workshopUpgrade3;
declare namespace technologies {
  export { technologies_advancedBelts as advancedBelts, technologies_atomicLocator as atomicLocator, technologies_batteries as batteries, circuitStatue$1 as circuitStatue, technologies_circuitry as circuitry, technologies_coalPower as coalPower, technologies_computers as computers, technologies_concrete as concrete, technologies_decorativeRocks as decorativeRocks, technologies_decorativeTrees as decorativeTrees, technologies_decorativeWalls as decorativeWalls, earthStatue$1 as earthStatue, earthTeleporter$1 as earthTeleporter, technologies_energyCube as energyCube, technologies_extractorUpgrade1 as extractorUpgrade1, technologies_extractorUpgrade2 as extractorUpgrade2, technologies_extractorUpgrade3 as extractorUpgrade3, technologies_extractorUpgrade4 as extractorUpgrade4, technologies_factoryDecorations as factoryDecorations, technologies_festiveDecorations as festiveDecorations, technologies_forgeUpgrade1 as forgeUpgrade1, technologies_forgeUpgrade2 as forgeUpgrade2, technologies_forgeUpgrade3 as forgeUpgrade3, technologies_furnaceUpgrade1 as furnaceUpgrade1, technologies_furnaceUpgrade2 as furnaceUpgrade2, technologies_furnaceUpgrade3 as furnaceUpgrade3, gearStatue$1 as gearStatue, gemTree$1 as gemTree, goldVault$1 as goldVault, technologies_industrialFactoryUpgrade1 as industrialFactoryUpgrade1, technologies_industrialFactoryUpgrade2 as industrialFactoryUpgrade2, technologies_industrialFactoryUpgrade3 as industrialFactoryUpgrade3, technologies_industrialFrame as industrialFrame, technologies_machineShopUpgrade1 as machineShopUpgrade1, technologies_machineShopUpgrade2 as machineShopUpgrade2, technologies_machineShopUpgrade3 as machineShopUpgrade3, technologies_magnets as magnets, technologies_manufacturerUpgrade1 as manufacturerUpgrade1, technologies_manufacturerUpgrade2 as manufacturerUpgrade2, technologies_manufacturerUpgrade3 as manufacturerUpgrade3, technologies_matterCompressor as matterCompressor, technologies_matterDuplication as matterDuplication, technologies_metalFrames as metalFrames, technologies_metalWorking as metalWorking, technologies_microscopes as microscopes, technologies_motors as motors, technologies_nanoWire as nanoWire, technologies_nuclearPower as nuclearPower, particleStatue$1 as particleStatue, technologies_quantumEntanglement as quantumEntanglement, technologies_roboticArmUpgrade1 as roboticArmUpgrade1, technologies_roboticArmUpgrade2 as roboticArmUpgrade2, technologies_roboticArmUpgrade3 as roboticArmUpgrade3, technologies_robotics as robotics, technologies_sand as sand, technologies_smelting as smelting, technologies_speedUpgradeI as speedUpgradeI, technologies_speedUpgradeII as speedUpgradeII, technologies_speedUpgradeIII as speedUpgradeIII, technologies_speedUpgradeIV as speedUpgradeIV, technologies_speedUpgradeIX as speedUpgradeIX, technologies_speedUpgradeV as speedUpgradeV, technologies_speedUpgradeVI as speedUpgradeVI, technologies_speedUpgradeVII as speedUpgradeVII, technologies_speedUpgradeVIII as speedUpgradeVIII, technologies_speedUpgradeX as speedUpgradeX, technologies_speedUpgradeXI as speedUpgradeXI, technologies_speedUpgradeXII as speedUpgradeXII, technologies_stabilizer as stabilizer, technologies_startingTech as startingTech, technologies_steelProduction as steelProduction, steelStatue$1 as steelStatue, storage$1 as storage, technologies_storageUpgrade1 as storageUpgrade1, technologies_storageUpgrade2 as storageUpgrade2, technologies_storageUpgrade3 as storageUpgrade3, technologies_superComputers as superComputers, technologies_tungstenProduction as tungstenProduction, technologies_turbocharger as turbocharger, technologies_undergroundBeltUpgrade1 as undergroundBeltUpgrade1, technologies_undergroundBeltUpgrade2 as undergroundBeltUpgrade2, technologies_uraniumProcessing as uraniumProcessing, technologies_woodFrame as woodFrame, technologies_woodPlanks as woodPlanks, technologies_workshopUpgrade1 as workshopUpgrade1, technologies_workshopUpgrade2 as workshopUpgrade2, technologies_workshopUpgrade3 as workshopUpgrade3 };
}

/**
 * @author Space.yg
 */

declare const fence: Decoration;
declare const concreteWall: Decoration;
declare const prettyTree: Decoration;
declare const firTree: Decoration;
declare const birchTree: Decoration;
declare const palmTree: Decoration;
declare const rock1: Decoration;
declare const rock2: Decoration;
declare const rock3: Decoration;
declare const hazardCone: Decoration;
declare const brokenPipe: Decoration;
declare const barrel: Decoration;
declare const woodenCrate: Decoration;
declare const gearStatue: Decoration;
declare const circuitStatue: Decoration;
declare const steelStatue: Decoration;
declare const particleStatue: Decoration;
declare const earthStatue: Decoration;
declare const pumpkin: Decoration;
declare const snowman: Decoration;

declare const decorations_barrel: typeof barrel;
declare const decorations_birchTree: typeof birchTree;
declare const decorations_brokenPipe: typeof brokenPipe;
declare const decorations_circuitStatue: typeof circuitStatue;
declare const decorations_concreteWall: typeof concreteWall;
declare const decorations_earthStatue: typeof earthStatue;
declare const decorations_fence: typeof fence;
declare const decorations_firTree: typeof firTree;
declare const decorations_gearStatue: typeof gearStatue;
declare const decorations_hazardCone: typeof hazardCone;
declare const decorations_palmTree: typeof palmTree;
declare const decorations_particleStatue: typeof particleStatue;
declare const decorations_prettyTree: typeof prettyTree;
declare const decorations_pumpkin: typeof pumpkin;
declare const decorations_rock1: typeof rock1;
declare const decorations_rock2: typeof rock2;
declare const decorations_rock3: typeof rock3;
declare const decorations_snowman: typeof snowman;
declare const decorations_steelStatue: typeof steelStatue;
declare const decorations_woodenCrate: typeof woodenCrate;
declare namespace decorations {
  export { decorations_barrel as barrel, decorations_birchTree as birchTree, decorations_brokenPipe as brokenPipe, decorations_circuitStatue as circuitStatue, decorations_concreteWall as concreteWall, decorations_earthStatue as earthStatue, decorations_fence as fence, decorations_firTree as firTree, decorations_gearStatue as gearStatue, decorations_hazardCone as hazardCone, decorations_palmTree as palmTree, decorations_particleStatue as particleStatue, decorations_prettyTree as prettyTree, decorations_pumpkin as pumpkin, decorations_rock1 as rock1, decorations_rock2 as rock2, decorations_rock3 as rock3, decorations_snowman as snowman, decorations_steelStatue as steelStatue, decorations_woodenCrate as woodenCrate };
}

/**
 * @author Space.yg
 */

/** *Place on resource nodes to extract infinite resources.* */
declare const extractor: Factory;
/** The output/min of {@link extractor Extractors}. */
declare const extractorOutputPerMin: 7.5;
/** *Place on resource nodes to extract infinite resources.* */
declare const uraniumExtractor: Factory;
/** The output/min of {@link uraniumExtractor Uranium Extractors}. */
declare const uraniumExtractorOutputPerMin: 10;
/** *Used to craft items automatically, select a recipe after building.* */
declare const workshop: Factory;
/** *Used to craft items automatically, select a recipe after building.* */
declare const furnace: Factory;
/** *Used to craft items automatically, select a recipe after building.* */
declare const machineShop: Factory;
/** *Used to craft items automatically, select a recipe after building.* */
declare const forge: Factory;
/** *Used to craft items automatically, select a recipe after building.* */
declare const industrialFactory: Factory;
/** *Used to craft items automatically, select a recipe after building.* */
declare const manufacturer: Factory;
/** *Used to craft items automatically, select a recipe after building.* */
declare const earthTeleporter: Factory;
/** *Money might not grow on trees but Gems do. Gem Apples can be harvested using a Robotic Arm. Send Gem Apples into the Research Lab or Gold Vault to get Gems.* */
declare const gemTree: Factory;
/** The output/min of {@link gemTree Gem Trees}. */
declare const gemTreeOutputPerMin: 0.2;

declare const factories_earthTeleporter: typeof earthTeleporter;
declare const factories_extractor: typeof extractor;
declare const factories_extractorOutputPerMin: typeof extractorOutputPerMin;
declare const factories_forge: typeof forge;
declare const factories_furnace: typeof furnace;
declare const factories_gemTree: typeof gemTree;
declare const factories_gemTreeOutputPerMin: typeof gemTreeOutputPerMin;
declare const factories_industrialFactory: typeof industrialFactory;
declare const factories_machineShop: typeof machineShop;
declare const factories_manufacturer: typeof manufacturer;
declare const factories_uraniumExtractor: typeof uraniumExtractor;
declare const factories_uraniumExtractorOutputPerMin: typeof uraniumExtractorOutputPerMin;
declare const factories_workshop: typeof workshop;
declare namespace factories {
  export { factories_earthTeleporter as earthTeleporter, factories_extractor as extractor, factories_extractorOutputPerMin as extractorOutputPerMin, factories_forge as forge, factories_furnace as furnace, factories_gemTree as gemTree, factories_gemTreeOutputPerMin as gemTreeOutputPerMin, factories_industrialFactory as industrialFactory, factories_machineShop as machineShop, factories_manufacturer as manufacturer, factories_uraniumExtractor as uraniumExtractor, factories_uraniumExtractorOutputPerMin as uraniumExtractorOutputPerMin, factories_workshop as workshop };
}

/**
 * @author Space.yg
 */

/** Item sent into the Research Lab can be used to unlock technology. Items not used for research will give you gold instead. */
declare const researchLab: ItemCollector;
/** Item sent into the Gold Vault converted into gold that can be used to build your factory. */
declare const goldVault: ItemCollector;

declare const itemCollectors_goldVault: typeof goldVault;
declare const itemCollectors_researchLab: typeof researchLab;
declare namespace itemCollectors {
  export { itemCollectors_goldVault as goldVault, itemCollectors_researchLab as researchLab };
}

/**
 * @author Space.yg
 */

declare const coalPowerPlant: PowerPlant;
declare const nuclearPowerPlant: PowerPlant;

declare const powerPlants_coalPowerPlant: typeof coalPowerPlant;
declare const powerPlants_nuclearPowerPlant: typeof nuclearPowerPlant;
declare namespace powerPlants {
  export { powerPlants_coalPowerPlant as coalPowerPlant, powerPlants_nuclearPowerPlant as nuclearPowerPlant };
}

/**
 * @author Space.yg
 */

/** *Storage buildings can only store one type of item at a time. Max capacity can be upgraded.* */
declare const storage: Storage;

declare const storages_storage: typeof storage;
declare namespace storages {
  export { storages_storage as storage };
}

/**
 * @author Space.yg
 */

/** Conveyor Belts transport items around the world. Connect to belts, extractors, factories, and other buildings. */
declare const conveyorBelt: TransportationSpeed;
/**
 * The speeds of the Conveyor Belt.
 * @readonly
 */
declare const BeltSpeeds: number[];
/** Underground Belts transport items underground, allowing them to avoid obstacles. Connect to another underground belt entrance / exist with up to 4 tiles of space between them. */
declare const undergroundBelt: TransportationDistance;
/**
 * The {@link undergroundBelt Underground Belt} distances.
 * @readonly
 */
declare const UndergroundBeltDistances: number[];
/** Belt Splitters evenly distribute items to connected belts or factories. */
declare const beltSplitter: TransportationDistance;
/** Robotic Arms can move items between buildings and belts from any direction. They can also filter for specific items. */
declare const roboticArm: TransportationSpeed;

declare const transportations_BeltSpeeds: typeof BeltSpeeds;
declare const transportations_UndergroundBeltDistances: typeof UndergroundBeltDistances;
declare const transportations_beltSplitter: typeof beltSplitter;
declare const transportations_conveyorBelt: typeof conveyorBelt;
declare const transportations_roboticArm: typeof roboticArm;
declare const transportations_undergroundBelt: typeof undergroundBelt;
declare namespace transportations {
  export { transportations_BeltSpeeds as BeltSpeeds, transportations_UndergroundBeltDistances as UndergroundBeltDistances, transportations_beltSplitter as beltSplitter, transportations_conveyorBelt as conveyorBelt, transportations_roboticArm as roboticArm, transportations_undergroundBelt as undergroundBelt };
}

/**
 * @author Space.yg
 */
interface DiscordUser {
    /** The user's id */
    id: string;
    /** The user's username, not unique across the platform */
    username: string;
    /** The user's Discord-tag */
    discriminator: string;
    /** The user's display name, if it is set. For bots, this is the application name */
    global_name: string | null;
    /** The user's avatar hash */
    avatar: string | null;
    /** Whether the user belongs to an OAuth2 application */
    bot?: boolean;
    /** Whether the user is an Official Discord System user (part of the urgent message system) */
    system?: boolean;
    /** Whether the user has two factor enabled on their account */
    mfa_enabled?: boolean;
    /** The user's banner hash */
    banner?: string | null;
    /** The user's banner color encoded as an integer representation of hexadecimal color code */
    accentColor?: number | null;
    /** The user's chosen language option */
    locale?: string;
    /** Whether the email on this account has been verified */
    verified?: boolean;
    /** The user's email */
    email?: string | null;
    /** The flags on a user's account */
    flags?: number;
    /** The type of Nitro subscription on a user's account */
    premium_type?: number;
    /** The public flags on a user's account */
    public_flags?: number;
    /** The user's avatar decoration hash */
    avatar_decoration?: string | null;
}
declare const Space: {
    /** The author a.k.a me :) */
    yg: {
        /** My Discord information */
        Discord: DiscordUser;
        /** My GitHub account */
        GitHub: string;
        /** My Instagram account */
        Instagram: string;
        /** My X (Twitter) account */
        X: string;
    };
};

export { type AdvancedWorldSetting, Area, type AreaParams, Base, type BaseParams, type BuildTierOptions, Decoration, type DecorationParams, DistanceTier, type DistanceTierParams, DistanceTiers, type DistanceTiersParams, Factory, type FactoryParams, type InputMap, type InputOptions, Item, ItemCollector, type ItemCollectorParams, type ItemParams, OutputTier, type OutputTierParams, OutputTiers, type OutputTiersParams, PowerPlant, type PowerPlantParams, Price, type PriceParams, type ResourcesParams, Seed, type SeedParams, Space, SpeedTier, type SpeedTierParams, SpeedTiers, type SpeedTiersParams, Storage, type StorageParams, StorageTier, type StorageTierParams, StorageTiers, type StorageTiersParams, Technology, type TechnologyParams, Tier, type TierParams, Tiers, type TiersParams, TransportationDistance, type TransportationDistanceParams, TransportationSpeed, type TransportationSpeedParams, currencies, decorations, factories, itemCollectors, items, storages as others, powerPlants, technologies, transportations };
